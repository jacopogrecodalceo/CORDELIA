# Cordelia's good instrument practice

- REMEMBER: an instrument NAME only exist ONCE (useful for naming variables)
- if the variable is used once and in general do not change for an inststrument --> make it GLOBAL !