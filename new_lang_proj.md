each: 1/4, 1/3, 1	-- this syntax is a block and it depends from the function name 
	@aaron.getmeout	-- maybe without the "@", but it will help recognising the instrument name - after the "." the possible output or routing fx
	beats		-- duration in seconds, beats will be a variable updated from bpm
	mf		-- dynamic
	b3		-- e.g. conversion in frequencies